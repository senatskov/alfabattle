spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: postgres
    password: password
  liquibase:
    change-log: classpath:liquibase/changelog.xml
    enabled: true
    contexts: default
